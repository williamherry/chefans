<%= form_for(@topic) do |f| %>

  <%= f.label :title %>
  <%= f.select :node_id, options_for_select(Node.all.map {|node| [node.name, node.id]}) %>
  <%= f.text_field :title %><br />

  <%= f.label :content %>
  <%= f.text_area :content %>

  <%= f.submit "Save" %>
<% end %>
